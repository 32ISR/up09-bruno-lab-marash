info:
  name: bids
  type: http
  seq: 4

http:
  method: GET
  url: "{{per}}/api/items/:id/bids"
  params:
    - name: id
      value: "1775108033027"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
