info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{per}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "user12345",
        "password": "password123",
        "email": "test@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
