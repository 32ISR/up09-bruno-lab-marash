info:
  name: delete
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{per}}/api/items/:id"
  params:
    - name: id
      value: "1775107422293"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
